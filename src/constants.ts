import { $item, $path, get } from "libram";

export const DRUNK_VOA = 3250;
export const DUPE_ITEM = $item`bottle of greedy dog`;
export const STASH_CLAN = "Alliance from Heck";

export const VOA = get("valueOfAdventure");
export const PATH = $path`Community Service`;
export const KRAMCO_CHANCE = 0.05;

export type Leg = "Aftercore" | "CS" | "Casual";
